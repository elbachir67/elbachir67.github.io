# Dépendances Python pour les pages à code exécutable (figures, calculs).
# Les résultats sont gelés dans _freeze/ (voir ADR-0001), ces paquets ne sont
# donc nécessaires que lorsqu'une page exécutable change.
jupyter>=1.0
numpy>=1.26
matplotlib>=3.8
