# Rapport de conversion — cm2_archi_seance2.tex

Produit par `scripts/importer_chapitre.py` (US-15). Sortie : `cours/architectures-logicielles-modernes/chapitres/02-l-hexagone.qmd`.

## À traiter à la main

Rien : tout le contenu a été converti.

## Figures

- figC_inversion : insérée, texte alternatif — texte fourni par le PO
- figD_hexagone : insérée, texte alternatif — texte fourni par le PO
- figE_swap : insérée, texte alternatif — texte fourni par le PO
- figF_chemin_lab2 : insérée, texte alternatif — légende du .tex

Nettoyage des SVG (encres en `currentColor`, fonds clairs transparents) :

- `figC_inversion` : 7 encre(s), 2 fond(s), 1 police(s)
- `figD_hexagone` : 16 encre(s), 5 fond(s), 1 police(s)
- `figE_swap` : 10 encre(s), 3 fond(s), 1 police(s)
- `figF_chemin_lab2` : 31 encre(s), 5 fond(s), 1 police(s)

## Blocs de code

- bloc 1 : `java`
- bloc 2 : `java`

## Tableaux

2 tableau(x) converti(s) en Markdown.

## Commandes de mise en page ignorées

Elles n'ont pas d'équivalent en HTML : le style s'en charge.

- `\addlinespace` : 2 fois
- `\bottomrule` : 2 fois
- `\centering` : 4 fois
- `\midrule` : 2 fois
- `\small` : 1 fois
- `\titlepage` : 1 fois
- `\toprule` : 2 fois
